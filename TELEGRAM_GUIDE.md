# دليل تشغيل نظام تيليجرام - SocialPro

## ✅ ما تم إنجازه (المرحلة الأولى)

تم تطوير الميزات التالية بنجاح:

### 1. قاعدة البيانات
- ✅ جدول `telegram_sessions` - لحفظ جلسات تيليجرام
- ✅ جدول `telegram_groups` - لحفظ المجموعات المستوردة
- ✅ RLS Policies - حماية أمنية كاملة
- ✅ Indexes - لتحسين الأداء

### 2. واجهة المستخدم (Frontend)
- ✅ صفحة **جلسات تيليجرام** (`/dashboard/telegram/sessions`)
  - عرض قائمة الجلسات
  - إضافة جلسة جديدة (Modal بخطوتين)
  - حذف جلسة
  
- ✅ صفحة **المجموعات** (`/dashboard/telegram/groups`)
  - عرض المجموعات المستوردة
  - استيراد مجموعات (Modal)
  - إحصائيات (إجمالي المجموعات، الأعضاء)
  - بحث في المجموعات

- ✅ رابط تيليجرام في Dashboard Sidebar

### 3. Backend API (Python)
- ✅ FastAPI + Telethon
- ✅ 4 Endpoints جاهزة:
  - `POST /auth/send-code` - إرسال رمز التحقق
  - `POST /auth/verify-code` - التحقق والحصول على session_string
  - `POST /groups/import/{session_id}` - استيراد المجموعات
  - `DELETE /sessions/{session_id}` - حذف جلسة

---

## 🚀 خطوات التشغيل

### المرحلة 1: نشر Backend على Render.com (مجاناً)

#### 1. رفع الكود على GitHub

```bash
cd /workspace/telegram-backend

# Initialize git
git init
git add .
git commit -m "Initial Telegram Backend"

# Create a new repository on GitHub first, then:
git remote add origin https://github.com/YOUR_USERNAME/telegram-backend.git
git push -u origin main
```

#### 2. نشر على Render.com

1. اذهب إلى [render.com](https://render.com)
2. سجل دخول بحساب GitHub
3. اضغط **New +** → **Web Service**
4. اختر repository: `telegram-backend`
5. املأ التفاصيل:
   - **Name**: `socialpro-telegram-backend`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Plan**: `Free` ✅

6. اضغط **Create Web Service**

#### 3. الحصول على URL Backend

بعد النشر، ستحصل على URL مثل:
```
https://socialpro-telegram-backend-xxxx.onrender.com
```

احتفظ بهذا الرابط! ستحتاجه في الخطوة التالية.

---

### المرحلة 2: ربط Frontend مع Backend

#### 1. تحديث متغيرات البيئة

في مشروع SocialPro Frontend، أضف ملف `.env.local`:

```bash
NEXT_PUBLIC_TELEGRAM_BACKEND_URL=https://socialpro-telegram-backend-xxxx.onrender.com
```

⚠️ **مهم:** استبدل الرابط بالرابط الفعلي من Render.com

#### 2. إعادة البناء والنشر

```bash
cd /workspace/socialpro-saas
npm run build
```

ثم انشر التطبيق مرة أخرى.

---

### المرحلة 3: الحصول على Telegram API Credentials

#### للمستخدمين:

1. اذهب إلى [my.telegram.org](https://my.telegram.org)
2. سجل دخول برقم هاتفك
3. اضغط **API Development Tools**
4. املأ النموذج:
   - **App title**: `SocialPro`
   - **Short name**: `socialpro`
   - **Platform**: `Other`
5. اضغط **Create application**

6. احفظ البيانات التالية:
   - **api_id**: مثل `12345678`
   - **api_hash**: مثل `abcdef1234567890abcdef1234567890`

⚠️ **تحذير:** لا تشارك هذه البيانات مع أي شخص!

---

## 📖 دليل الاستخدام

### 1. إضافة جلسة تيليجرام جديدة

1. اذهب إلى **Dashboard** → **تيليجرام**
2. اضغط **إضافة جلسة جديدة**
3. املأ البيانات:
   - **اسم الجلسة**: مثل "حساب العمل"
   - **رقم الهاتف**: بصيغة دولية (مثل: +966xxxxxxxxx)
   - **API ID**: من my.telegram.org
   - **API Hash**: من my.telegram.org
4. اضغط **التالي**
5. أدخل **رمز التحقق** المرسل إلى تيليجرام
6. اضغط **تحقق**

✅ تم! الجلسة الآن نشطة.

### 2. استيراد مجموعات تيليجرام

1. اذهب إلى **Dashboard** → **تيليجرام** → **المجموعات**
2. اضغط **استيراد مجموعات**
3. اختر الجلسة التي تريد استيراد المجموعات منها
4. اضغط **استيراد المجموعات**

✅ سيتم استيراد جميع المجموعات التي أنت عضو فيها!

### 3. عرض وإدارة المجموعات

في صفحة المجموعات، يمكنك:
- 🔍 **البحث** في المجموعات
- 📊 **عرض الإحصائيات** (عدد المجموعات، الأعضاء)
- 🗑️ **حذف** مجموعة

---

## 🔧 استكشاف الأخطاء

### مشكلة: Backend لا يستجيب

**الحل:**
- Backend على Render.com ينام بعد 15 دقيقة من عدم النشاط
- أول طلب قد يأخذ 30-60 ثانية
- انتظر قليلاً وحاول مرة أخرى

### مشكلة: "رمز التحقق غير صحيح"

**الحل:**
- تأكد من إدخال الرمز بشكل صحيح (5 أرقام)
- الرمز صالح لمدة محدودة، إذا انتهى اطلب رمز جديد

### مشكلة: "فشل استيراد المجموعات"

**الحل:**
- تأكد من أن الجلسة نشطة (Status = "نشط")
- تأكد من اتصالك بالإنترنت
- تحقق من أن Backend يعمل

---

## 🎯 الميزات القادمة (المراحل التالية)

المرحلة الأولى (الحالية) تشمل:
- ✅ إدارة الجلسات
- ✅ استيراد المجموعات الأساسي
- ✅ عرض وبحث المجموعات

### المراحل القادمة ستشمل:
- 🔜 **البحث المتقدم** عن مجموعات جديدة (بالكلمات المفتاحية)
- 🔜 **استخراج الأعضاء** من المجموعات
- 🔜 **نظام الحملات التسويقية** (إرسال رسائل جماعية)
- 🔜 **إحصائيات متقدمة** في Dashboard

---

## 📂 هيكل الملفات

### Frontend (Next.js/React)
```
socialpro-saas/
├── app/
│   └── dashboard/
│       └── telegram/
│           ├── sessions/page.tsx  (صفحة الجلسات)
│           └── groups/page.tsx    (صفحة المجموعات)
├── lib/
│   └── telegram-api.ts            (مكتبة الاتصال بـ Backend)
└── supabase/
    └── migrations/                (قاعدة البيانات)
```

### Backend (Python/FastAPI)
```
telegram-backend/
├── main.py                (FastAPI app + Telethon)
├── requirements.txt       (المكتبات المطلوبة)
├── README.md             (دليل النشر)
└── .env.example          (مثال لمتغيرات البيئة)
```

---

## 🔐 الأمان

### بيانات حساسة:
- ✅ API credentials محفوظة بشكل آمن في قاعدة البيانات
- ✅ RLS Policies تضمن أن كل مستخدم يرى جلساته فقط
- ✅ session_string مشفر ومحمي

### نصائح أمنية:
- 🔒 لا تشارك API ID/Hash مع أي شخص
- 🔒 لا تحفظ بيانات الاعتماد في الكود
- 🔒 استخدم Environment Variables فقط

---

## 📞 الدعم

في حال واجهت أي مشاكل:

1. **تحقق من Logs في Render Dashboard**
2. **تحقق من Console في المتصفح** (F12)
3. **تأكد من صحة API credentials**
4. **تأكد من رقم الهاتف بالصيغة الدولية**

---

## 🎉 جاهز للاستخدام!

التطبيق الآن منشور على:
**https://ebg8lgsmf5qe.space.minimax.io**

للبدء:
1. سجل دخول إلى حسابك
2. اذهب إلى قسم **تيليجرام** من Sidebar
3. أضف جلسة جديدة
4. ابدأ في استيراد مجموعاتك!

**ملاحظة:** لتشغيل الميزات بالكامل، يجب نشر Backend على Render.com أولاً (الخطوات أعلاه).

---

**تم التطوير بواسطة:** MiniMax Agent  
**التاريخ:** 2025-11-02  
**الإصدار:** 1.0.0 (المرحلة الأولى)
