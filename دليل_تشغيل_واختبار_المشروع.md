# 🚀 دليل تشغيل واختبار المشروع - SocialProMax

**تاريخ الدليل:** 2025-11-03

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- ✅ **Node.js** 18+ ([تحميل](https://nodejs.org/))
- ✅ **Python** 3.9+ ([تحميل](https://www.python.org/))
- ✅ **Git** ([تحميل](https://git-scm.com/))
- ✅ حساب **Supabase** ([إنشاء حساب](https://supabase.com))
- ✅ حساب **Telegram** (لاختبار Telegram Features)

---

## 🎯 الخطوة 1: إعداد Supabase

### 1.1 الحصول على مفاتيح Supabase

1. اذهب إلى: https://supabase.com/dashboard/project/gigrtzamstdyynmvwljq
2. سجل دخول
3. اذهب إلى **Settings** > **API**
4. احفظ:
   - ✅ **Project URL**: `https://gigrtzamstdyynmvwljq.supabase.co`
   - ✅ **anon/public key**: ابدأ بـ `eyJhbGc...`
   - ✅ **service_role key**: ابدأ بـ `eyJhbGc...` (⚠️ لا تشاركها)

### 1.2 إنشاء الجداول الجديدة

إذا لم تكن موجودة، قم بإنشائها:

#### جدول telegram_campaign_messages:
```sql
-- انسخ محتوى الملف: supabase/tables/telegram_campaign_messages.sql
-- والصقه في SQL Editor في Supabase Dashboard
```

#### جدول telegram_members:
```sql
-- انسخ محتوى الملف: supabase/tables/telegram_members.sql
-- والصقه في SQL Editor في Supabase Dashboard
```

**الطريقة:**
1. اذهب إلى Supabase Dashboard > **SQL Editor**
2. أنشئ Query جديد
3. انسخ محتوى ملف SQL
4. الصقه
5. اضغط **Run**

### 1.3 نشر Edge Functions (اختياري - للاختبار المحلي)

إذا كنت تريد اختبار Edge Functions محلياً:

```bash
# تثبيت Supabase CLI
npm install -g supabase

# تسجيل الدخول
supabase login

# ربط المشروع
supabase link --project-ref gigrtzamstdyynmvwljq

# نشر Functions
supabase functions deploy telegram-send-message
supabase functions deploy telegram-extract-members
supabase functions deploy telegram-transfer-members
```

**أو يمكنك نشرها من Dashboard:**
1. اذهب إلى **Edge Functions** في Supabase Dashboard
2. ارفع كل function يدوياً

---

## 🎯 الخطوة 2: إعداد Frontend (Next.js)

### 2.1 الانتقال إلى مجلد المشروع

```bash
cd SocialProMax/socialpro-saas
```

### 2.2 تثبيت Dependencies

```bash
npm install
```

**الوقت المتوقع:** 2-5 دقائق

### 2.3 إنشاء ملف Environment Variables

أنشئ ملف `.env.local` في مجلد `socialpro-saas/`:

```env
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://gigrtzamstdyynmvwljq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Telegram Backend (اختياري - إذا كنت تريد اختبار Telegram)
NEXT_PUBLIC_TELEGRAM_BACKEND_URL=http://localhost:8000

# Site URL (اختياري)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**⚠️ مهم:** استبدل `your_anon_key_here` بالمفتاح الفعلي من Supabase Dashboard

### 2.4 تشغيل المشروع

```bash
npm run dev
```

**النتيجة:**
- ✅ المشروع سيعمل على: http://localhost:3000
- ✅ افتح المتصفح واذهب إلى الرابط

---

## 🎯 الخطوة 3: إعداد Telegram Backend (اختياري)

⚠️ **ملاحظة:** هذا اختياري. إذا كنت تريد اختبار ميزات Telegram فقط.

### 3.1 الانتقال إلى مجلد Backend

```bash
cd SocialProMax/telegram-backend
```

### 3.2 تثبيت Dependencies

```bash
pip install -r requirements.txt
```

### 3.3 تشغيل Backend

```bash
python main.py
```

**النتيجة:**
- ✅ Backend سيعمل على: http://localhost:8000
- ✅ يمكنك اختبار Health: http://localhost:8000/health

---

## 🧪 الخطوة 4: الاختبار

### 4.1 اختبار الصفحات الأساسية

#### أ) الصفحة الرئيسية
1. اذهب إلى: http://localhost:3000
2. تحقق من:
   - ✅ الصفحة تحمل بشكل صحيح
   - ✅ التصميم يظهر بشكل جيد
   - ✅ الروابط تعمل

#### ب) صفحة التسجيل
1. اذهب إلى: http://localhost:3000/signup
2. جرب إنشاء حساب جديد:
   - أدخل بريد إلكتروني
   - أدخل كلمة مرور
   - اضغط "إنشاء حساب"
3. تحقق من:
   - ✅ رسالة تأكيد من Supabase
   - ✅ إعادة توجيه إلى Dashboard

#### ج) صفحة تسجيل الدخول
1. اذهب إلى: http://localhost:3000/login
2. سجل دخول بالحساب الذي أنشأته
3. تحقق من:
   - ✅ تسجيل الدخول بنجاح
   - ✅ إعادة توجيه إلى Dashboard

### 4.2 اختبار Dashboard

#### أ) Dashboard الرئيسية
1. بعد تسجيل الدخول، اذهب إلى: http://localhost:3000/dashboard
2. تحقق من:
   - ✅ الإحصائيات تظهر
   - ✅ الرسوم البيانية تعمل
   - ✅ القائمة الجانبية تظهر

#### ب) صفحة المنصات
1. اذهب إلى: http://localhost:3000/dashboard/platforms
2. تحقق من:
   - ✅ المنصات تظهر (12 منصة)
   - ✅ يمكنك ربط/فصل المنصات

#### ج) صفحة الحملات
1. اذهب إلى: http://localhost:3000/dashboard/campaigns
2. تحقق من:
   - ✅ القائمة تظهر
   - ✅ يمكنك إنشاء حملة جديدة

### 4.3 اختبار Telegram (اختياري)

⚠️ **مطلوب:** Telegram Backend يجب أن يعمل

#### أ) إضافة جلسة Telegram
1. احصل على API Credentials من [my.telegram.org](https://my.telegram.org)
2. اذهب إلى: http://localhost:3000/dashboard/telegram/sessions
3. اضغط "إضافة جلسة جديدة"
4. املأ البيانات:
   - اسم الجلسة
   - رقم الهاتف (+966...)
   - API ID
   - API Hash
5. اضغط "التالي"
6. أدخل رمز التحقق من Telegram
7. تحقق من:
   - ✅ الجلسة تم إنشاؤها بنجاح
   - ✅ تظهر في القائمة

#### ب) استيراد المجموعات
1. اذهب إلى: http://localhost:3000/dashboard/telegram/groups
2. اضغط "استيراد مجموعاتي"
3. اختر الجلسة
4. اضغط "استيراد الآن"
5. تحقق من:
   - ✅ المجموعات تم استيرادها
   - ✅ تظهر في القائمة

#### ج) إرسال رسائل (الحملات)
1. اذهب إلى: http://localhost:3000/dashboard/telegram/campaigns
2. اضغط "إنشاء حملة جديدة"
3. املأ:
   - اسم الحملة
   - نص الرسالة
   - اختر الجلسة
   - اختر المجموعات
4. اضغط "إرسال الآن"
5. تحقق من:
   - ✅ الرسائل تم إرسالها
   - ✅ تظهر في قائمة الحملات

#### د) استخراج الأعضاء
1. اذهب إلى: http://localhost:3000/dashboard/telegram/members-extraction
2. اختر مجموعة
3. اضغط "استخراج الأعضاء"
4. اختر الجلسة
5. اضغط "بدء الاستخراج"
6. تحقق من:
   - ✅ الأعضاء تم استخراجهم
   - ✅ يمكن تصدير CSV

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: خطأ في الاتصال بـ Supabase

**الأعراض:**
- رسالة خطأ: "Failed to fetch"
- صفحة لا تحمل

**الحل:**
1. تحقق من `.env.local` موجود
2. تحقق من `NEXT_PUBLIC_SUPABASE_URL` صحيح
3. تحقق من `NEXT_PUBLIC_SUPABASE_ANON_KEY` صحيح
4. أعد تشغيل السيرفر: `npm run dev`

### المشكلة 2: خطأ في تسجيل الدخول

**الأعراض:**
- لا يمكن تسجيل الدخول
- رسالة خطأ من Supabase

**الحل:**
1. تحقق من Supabase Dashboard > **Authentication**
2. تأكد من تفعيل Email/Password provider
3. تحقق من إعدادات Email Templates

### المشكلة 3: Telegram Backend لا يعمل

**الأعراض:**
- خطأ عند إرسال رسائل
- خطأ عند استخراج أعضاء

**الحل:**
1. تحقق من Backend يعمل: http://localhost:8000/health
2. تحقق من `NEXT_PUBLIC_TELEGRAM_BACKEND_URL` في `.env.local`
3. تأكد من تثبيت جميع المتطلبات: `pip install -r requirements.txt`

### المشكلة 4: الجداول غير موجودة

**الأعراض:**
- خطأ 404 عند جلب البيانات
- رسالة "Table not found"

**الحل:**
1. اذهب إلى Supabase Dashboard > **SQL Editor**
2. نفذ ملفات SQL من `supabase/tables/`
3. تحقق من إنشاء الجداول بنجاح

---

## 📊 قائمة التحقق النهائية

### قبل الاختبار:

- [ ] Node.js مثبت (18+)
- [ ] Python مثبت (3.9+)
- [ ] حساب Supabase موجود
- [ ] مفاتيح Supabase جاهزة
- [ ] ملف `.env.local` موجود
- [ ] Dependencies مثبتة (`npm install`)

### أثناء الاختبار:

- [ ] الصفحة الرئيسية تعمل
- [ ] يمكن التسجيل
- [ ] يمكن تسجيل الدخول
- [ ] Dashboard يظهر
- [ ] المنصات تظهر
- [ ] الحملات تعمل (اختياري)
- [ ] Telegram يعمل (اختياري)

---

## 🎯 خطوات الاختبار السريع

### اختبار سريع (5 دقائق):

```bash
# 1. تثبيت Dependencies
cd SocialProMax/socialpro-saas
npm install

# 2. إنشاء .env.local
# (انسخ من المثال أعلاه)

# 3. تشغيل المشروع
npm run dev

# 4. افتح المتصفح
# http://localhost:3000
```

### اختبار شامل (30 دقيقة):

1. ✅ اختبار جميع الصفحات
2. ✅ اختبار التسجيل والدخول
3. ✅ اختبار Dashboard
4. ✅ اختبار Telegram (إذا كان Backend جاهز)
5. ✅ اختبار إرسال رسائل
6. ✅ اختبار استخراج أعضاء
7. ✅ اختبار نقل أعضاء

---

## 📝 ملاحظات مهمة

### للتطوير المحلي:
- ✅ استخدام `npm run dev` للتطوير
- ✅ المشروع يعمل على `http://localhost:3000`
- ✅ Hot Reload مفعّل تلقائياً

### للإنتاج:
- ✅ استخدام `npm run build` ثم `npm start`
- ✅ تأكد من جميع Environment Variables موجودة
- ✅ تأكد من نشر Edge Functions على Supabase

### للاختبار:
- ✅ استخدم حساب تجريبي في Supabase
- ✅ لا تستخدم بيانات حقيقية في التطوير
- ✅ احتفظ بنسخة احتياطية من البيانات

---

## ✅ الخلاصة

**الخطوات الأساسية:**

1. ✅ إعداد Supabase (الحصول على المفاتيح)
2. ✅ إنشاء `.env.local`
3. ✅ تثبيت Dependencies (`npm install`)
4. ✅ تشغيل المشروع (`npm run dev`)
5. ✅ فتح المتصفح (http://localhost:3000)
6. ✅ الاختبار

**الوقت المتوقع:** 15-30 دقيقة

---

**تم إنشاء الدليل:** 2025-11-03  
**الحالة:** ✅ جاهز للاستخدام

