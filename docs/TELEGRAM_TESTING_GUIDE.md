# دليل اختبار شامل - نظام Telegram المتقدم

**تاريخ الإنشاء:** 2025-01-05  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للاختبار

---

## 📋 نظرة عامة

هذا الدليل يغطي اختبار شامل لجميع الميزات الجديدة التي تم إضافتها في المراحل 4 و 5:
- Edge Functions (6 functions)
- Frontend Pages (2 pages)
- Database Tables & RLS Policies
- Backend API Integration

---

## 🎯 أهداف الاختبار

1. **التحقق من الوظائف:**
   - جميع Edge Functions تعمل بشكل صحيح
   - Frontend يتفاعل بشكل صحيح مع Edge Functions
   - قاعدة البيانات تحفظ البيانات بشكل صحيح

2. **التحقق من الأمان:**
   - RLS Policies تعمل بشكل صحيح
   - المستخدمون لا يمكنهم الوصول لبيانات غيرهم
   - المصادقة مطلوبة لجميع العمليات

3. **التحقق من الأداء:**
   - التوزيع الذكي يعمل بشكل صحيح
   - التأخير الذكي يطبق بشكل عشوائي
   - الفلترة والاستبعاد تعمل بشكل صحيح

4. **التحقق من التكامل:**
   - جميع المكونات تعمل معاً بسلاسة
   - لا يوجد تعارض مع الميزات الموجودة

---

## 🧪 اختبار Edge Functions

### 1. `telegram-campaign-create`

**الهدف:** اختبار إنشاء حملة جديدة

**خطوات الاختبار:**
1. تسجيل الدخول إلى النظام
2. الانتقال إلى صفحة Campaigns
3. النقر على "إنشاء حملة جديدة"
4. ملء النموذج:
   - اسم الحملة: "اختبار حملة 1"
   - نوع الحملة: "مجموعات"
   - نص الرسالة: "رسالة اختبار"
   - اختيار مجموعة واحدة على الأقل
   - اختيار جلسة واحدة على الأقل
   - تفعيل خيارات الاستبعاد (Bots, Scam, Fake)
   - تعيين التأخير (30-90 ثانية)
5. النقر على "إنشاء الحملة"

**النتائج المتوقعة:**
- ✅ يتم إنشاء الحملة في جدول `telegram_campaigns`
- ✅ الحالة الافتراضية: `draft`
- ✅ يتم حفظ جميع الإعدادات بشكل صحيح
- ✅ رسالة نجاح تظهر للمستخدم

**التحقق من قاعدة البيانات:**
```sql
SELECT * FROM telegram_campaigns 
WHERE name = 'اختبار حملة 1' 
ORDER BY created_at DESC 
LIMIT 1;
```

**المشاكل المحتملة:**
- ❌ فشل في إنشاء الحملة → التحقق من Edge Function logs
- ❌ البيانات غير محفوظة → التحقق من RLS Policies
- ❌ خطأ في المصادقة → التحقق من user_id

---

### 2. `telegram-campaign-start`

**الهدف:** اختبار بدء حملة

**خطوات الاختبار:**
1. إنشاء حملة (أو استخدام حملة موجودة بحالة `draft`)
2. النقر على زر "بدء" في قائمة الحملات
3. تأكيد العملية

**النتائج المتوقعة:**
- ✅ حالة الحملة تتغير من `draft` إلى `active`
- ✅ `started_at` يتم تعيينه
- ✅ رسالة نجاح تظهر

**التحقق من قاعدة البيانات:**
```sql
SELECT status, started_at 
FROM telegram_campaigns 
WHERE id = 'campaign_id_here';
```

**المشاكل المحتملة:**
- ❌ الحملة لا تبدأ → التحقق من Edge Function
- ❌ حالة لا تتغير → التحقق من RLS Policies

---

### 3. `telegram-campaign-send-batch`

**الهدف:** اختبار إرسال دفعات من الرسائل

**خطوات الاختبار:**
1. إنشاء حملة بنوع "مجموعات"
2. اختيار مجموعة واحدة على الأقل
3. اختيار جلسة واحدة على الأقل
4. بدء الحملة
5. مراقبة عملية الإرسال

**النتائج المتوقعة:**
- ✅ يتم إرسال الرسائل إلى المجموعات المحددة
- ✅ يتم تسجيل كل رسالة في `telegram_campaign_messages`
- ✅ يتم تحديث `sent_count` و `failed_count` في الحملة
- ✅ يتم تطبيق التأخير بين الرسائل

**التحقق من قاعدة البيانات:**
```sql
-- التحقق من الرسائل المرسلة
SELECT * FROM telegram_campaign_messages 
WHERE campaign_id = 'campaign_id_here'
ORDER BY created_at DESC;

-- التحقق من إحصائيات الحملة
SELECT sent_count, failed_count, total_targets 
FROM telegram_campaigns 
WHERE id = 'campaign_id_here';
```

**المشاكل المحتملة:**
- ❌ الرسائل لا تُرسل → التحقق من Telegram Backend
- ❌ التأخير لا يطبق → التحقق من Edge Function logic
- ❌ التوزيع لا يعمل → التحقق من distribution strategy

---

### 4. `telegram-campaign-pause`

**الهدف:** اختبار إيقاف حملة مؤقتاً

**خطوات الاختبار:**
1. إنشاء حملة بحالة `active`
2. النقر على زر "إيقاف"
3. تأكيد العملية

**النتائج المتوقعة:**
- ✅ حالة الحملة تتغير إلى `paused`
- ✅ `paused_at` يتم تعيينه
- ✅ رسالة نجاح تظهر

**التحقق من قاعدة البيانات:**
```sql
SELECT status, paused_at 
FROM telegram_campaigns 
WHERE id = 'campaign_id_here';
```

---

### 5. `telegram-campaign-resume`

**الهدف:** اختبار استئناف حملة

**خطوات الاختبار:**
1. إنشاء حملة بحالة `paused`
2. النقر على زر "استئناف"
3. تأكيد العملية

**النتائج المتوقعة:**
- ✅ حالة الحملة تتغير إلى `active`
- ✅ `paused_at` يتم مسحه
- ✅ رسالة نجاح تظهر

---

### 6. `telegram-transfer-members-batch`

**الهدف:** اختبار نقل أعضاء بين مجموعتين

**خطوات الاختبار:**
1. الانتقال إلى صفحة Member Transfer
2. اختيار مجموعة مصدر
3. اختيار مجموعة هدف
4. اختيار أعضاء للنقل (اختيار واحد على الأقل)
5. اختيار جلسات (اختياري - أو استخدام جميع الجلسات)
6. تعيين استراتيجية التوزيع
7. تعيين التأخير (60-120 ثانية)
8. النقر على "بدء النقل"

**النتائج المتوقعة:**
- ✅ يتم نقل الأعضاء المحددين
- ✅ يتم تسجيل كل عملية نقل في `telegram_member_transfers`
- ✅ يتم تطبيق التأخير بين العمليات
- ✅ التوزيع يعمل بشكل صحيح بين الجلسات

**التحقق من قاعدة البيانات:**
```sql
-- التحقق من عمليات النقل
SELECT * FROM telegram_member_transfers 
WHERE source_group_id = 'source_group_id' 
  AND target_group_id = 'target_group_id'
ORDER BY created_at DESC;
```

**المشاكل المحتملة:**
- ❌ الأعضاء لا يُنقلون → التحقق من Telegram Backend
- ❌ التوزيع لا يعمل → التحقق من distribution strategy
- ❌ التأخير لا يطبق → التحقق من delay settings

---

## 🖥️ اختبار Frontend

### 1. صفحة Campaigns

**اختبار واجهة المستخدم:**

1. **عرض الحملات:**
   - ✅ عرض جميع الحملات بشكل صحيح
   - ✅ إحصائيات صحيحة (إجمالي، نشطة، مرسلة، معدل النجاح)
   - ✅ حالة كل حملة تظهر بشكل صحيح

2. **إنشاء حملة:**
   - ✅ النموذج يفتح بشكل صحيح
   - ✅ جميع الحقول متاحة
   - ✅ اختيار عدة جلسات يعمل
   - ✅ اختيار عدة مجموعات يعمل
   - ✅ خيارات الاستبعاد تعمل
   - ✅ إعدادات التأخير تعمل
   - ✅ التحقق من صحة البيانات يعمل

3. **إدارة الحملات:**
   - ✅ زر "بدء" يعمل للحملات في حالة `draft` أو `scheduled`
   - ✅ زر "إيقاف" يعمل للحملات في حالة `active`
   - ✅ زر "استئناف" يعمل للحملات في حالة `paused`
   - ✅ زر "تحديث" يعمل بشكل صحيح

4. **عرض التقدم:**
   - ✅ شريط التقدم يظهر بشكل صحيح
   - ✅ الإحصائيات محدثة
   - ✅ عدد الرسائل المرسلة والفاشلة صحيح

---

### 2. صفحة Member Transfer

**اختبار واجهة المستخدم:**

1. **اختيار المجموعات:**
   - ✅ اختيار مجموعة مصدر يعمل
   - ✅ اختيار مجموعة هدف يعمل
   - ✅ عرض الأعضاء المتاحة يعمل

2. **اختيار الأعضاء:**
   - ✅ تحديد عضو واحد يعمل
   - ✅ تحديد جميع الأعضاء يعمل
   - ✅ إلغاء التحديد يعمل

3. **الإعدادات المتقدمة:**
   - ✅ اختيار عدة جلسات يعمل
   - ✅ استراتيجية التوزيع تعمل
   - ✅ إعدادات التأخير تعمل

4. **عملية النقل:**
   - ✅ زر "بدء النقل" يعمل
   - ✅ شريط التقدم يظهر
   - ✅ رسالة نجاح/فشل تظهر

---

## 🗄️ اختبار قاعدة البيانات

### 1. اختبار RLS Policies

**الهدف:** التحقق من أن المستخدمين لا يمكنهم الوصول لبيانات غيرهم

**اختبارات:**

1. **`telegram_campaigns`:**
```sql
-- تسجيل الدخول كمستخدم 1
-- محاولة الوصول لحملات مستخدم آخر
SELECT * FROM telegram_campaigns 
WHERE user_id != auth.uid();

-- النتيجة المتوقعة: لا تظهر أي سجلات
```

2. **`telegram_sent_members`:**
```sql
-- محاولة الوصول لأعضاء مرسل لهم من مستخدم آخر
SELECT * FROM telegram_sent_members 
WHERE user_id != auth.uid();

-- النتيجة المتوقعة: لا تظهر أي سجلات
```

3. **`telegram_member_transfers`:**
```sql
-- محاولة الوصول لعمليات نقل من مستخدم آخر
SELECT * FROM telegram_member_transfers 
WHERE user_id != auth.uid();

-- النتيجة المتوقعة: لا تظهر أي سجلات
```

---

### 2. اختبار Constraints

**الهدف:** التحقق من أن القيود تعمل بشكل صحيح

**اختبارات:**

1. **UNIQUE constraint على `telegram_sent_members`:**
```sql
-- محاولة إدراج نفس العضو مرتين لنفس الحملة
INSERT INTO telegram_sent_members (user_id, campaign_id, member_telegram_id, sent_at)
VALUES 
  ('user_id', 'campaign_id', 123456, NOW()),
  ('user_id', 'campaign_id', 123456, NOW());

-- النتيجة المتوقعة: فشل في الإدراج الثاني
```

2. **CHECK constraints على `status`:**
```sql
-- محاولة إدراج حالة غير صحيحة
INSERT INTO telegram_campaigns (user_id, name, campaign_type, message_text, status, session_ids)
VALUES ('user_id', 'test', 'groups', 'test', 'invalid_status', '[]'::jsonb);

-- النتيجة المتوقعة: فشل في الإدراج
```

---

## 🔗 اختبار التكامل

### 1. اختبار التكامل بين Frontend و Edge Functions

**سيناريو كامل:**

1. **إنشاء حملة من Frontend:**
   - فتح صفحة Campaigns
   - إنشاء حملة جديدة
   - التحقق من أن الحملة تم إنشاؤها في قاعدة البيانات

2. **بدء الحملة:**
   - النقر على زر "بدء"
   - التحقق من تغيير الحالة في قاعدة البيانات

3. **مراقبة الإرسال:**
   - التحقق من أن الرسائل تُرسل
   - التحقق من تحديث الإحصائيات في الوقت الفعلي

---

### 2. اختبار التكامل مع Telegram Backend

**الهدف:** التحقق من أن Edge Functions تتواصل بشكل صحيح مع Telegram Backend

**اختبارات:**

1. **اختبار إرسال رسالة:**
   - Edge Function تستدعي `/messages/send`
   - التحقق من أن الرسالة تُرسل فعلياً على Telegram

2. **اختبار نقل عضو:**
   - Edge Function تستدعي `/members/transfer-batch`
   - التحقق من أن العضو يُنقل فعلياً

---

## 📊 تقرير الاختبار

### قالب التقرير:

```markdown
# تقرير اختبار - نظام Telegram المتقدم

**التاريخ:** [تاريخ الاختبار
**المختبر:** [اسم المختبر]
**البيئة:** [Development/Production]

## النتائج الإجمالية

- ✅ **Edge Functions:** X/6 نجحت
- ✅ **Frontend Pages:** X/2 نجحت
- ✅ **Database Tests:** X/X نجحت
- ✅ **Integration Tests:** X/X نجحت

## التفاصيل

### Edge Functions
1. telegram-campaign-create: ✅/❌
2. telegram-campaign-start: ✅/❌
3. telegram-campaign-send-batch: ✅/❌
4. telegram-campaign-pause: ✅/❌
5. telegram-campaign-resume: ✅/❌
6. telegram-transfer-members-batch: ✅/❌

### Frontend
1. صفحة Campaigns: ✅/❌
2. صفحة Member Transfer: ✅/❌

### قاعدة البيانات
1. RLS Policies: ✅/❌
2. Constraints: ✅/❌

### التكامل
1. Frontend ↔ Edge Functions: ✅/❌
2. Edge Functions ↔ Telegram Backend: ✅/❌

## المشاكل المكتشفة

1. [وصف المشكلة]
   - **الخطورة:** [عالية/متوسطة/منخفضة]
   - **الحل المقترح:** [وصف الحل]

## التوصيات

1. [توصية 1]
2. [توصية 2]
```

---

## ✅ Checklist نهائي

### قبل الإنتاج:
- [ ] جميع Edge Functions تم اختبارها
- [ ] جميع صفحات Frontend تم اختبارها
- [ ] RLS Policies تعمل بشكل صحيح
- [ ] التكامل بين جميع المكونات يعمل
- [ ] لا توجد أخطاء حرجة
- [ ] الأداء مقبول
- [ ] الأمان محقق
- [ ] التوثيق محدث

---

## 📝 ملاحظات إضافية

- **بيئة الاختبار:** يُفضل استخدام بيئة تطوير منفصلة قبل الإنتاج
- **البيانات:** استخدام بيانات اختبار وليس بيانات حقيقية
- **النسخ الاحتياطية:** أخذ نسخة احتياطية قبل الاختبار
- **التوثيق:** توثيق أي مشاكل أو ملاحظات أثناء الاختبار

---

**آخر تحديث:** 2025-01-05

